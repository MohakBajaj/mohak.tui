{
  "name": "mohak.tui",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:termux": "./scripts/build-termux.sh",
    "dev": "turbo run dev --filter=@mohak/tui-server",
    "dev:tui": "turbo run dev --filter=@mohak/tui-server",
    "start": "turbo run start --filter=@mohak/tui-server",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
